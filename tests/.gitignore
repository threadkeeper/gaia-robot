# Test output from the data-retrieval / data-execution self-tests
# (regenerated on each run).
LLM1/
LLM2/*

# Keep the authored push-pass spec — it is a contract document, not output.
!LLM2/DataExecutionSpec.md
