# app/

Application-level assets, configuration, and runtime resources (e.g. config
files, static assets, sample data) used by the program at runtime.

This folder is intentionally a placeholder for now.
