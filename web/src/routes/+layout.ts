// The whole app is a prerendered static shell (SPA) cached at the edge;
// all dynamic data is fetched client-side from the Gaia API at runtime.
export const prerender = true;
export const ssr = false;
